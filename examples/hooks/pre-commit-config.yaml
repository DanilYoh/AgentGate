repos:
  - repo: local
    hooks:
      - id: agentgate
        name: AgentGate
        entry: npm exec --offline -- agentgate check --staged
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
